apiVersion: v1
kind: Service
metadata:
  name: redis-agent-memory-ilb
  namespace: ram
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: redis-agent-memory
  ports:
    - name: http
      port: 9000
      targetPort: 9000
      protocol: TCP
